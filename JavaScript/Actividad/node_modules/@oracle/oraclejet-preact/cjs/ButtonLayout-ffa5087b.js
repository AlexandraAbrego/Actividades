/* @oracle/oraclejet-preact: 15.1.3 */
'use strict';

var jsxRuntime = require('preact/jsx-runtime');

require('./utils/UNSAFE_classNames.js');
require('./UNSAFE_Text.js');
var classNames = require('./classNames-58437073.js');
var Text = require('./Text-e57cfc67.js');

const compStyles = {
  text: "_875cer",
  suffix: "_1bljqmx",
  icon: "_1ky3hxu",
  startText: "xxguez",
  endText: "_1ry2hxp",
  startIcon: "juan3e",
  startIconWhenBothIconsOnly: "_1xoygpk",
  endIcon: "_1h42hoc",
  label: "_199kvho"
};
const isNullOrUndefined = value => {
  return value === null || value === undefined;
};
function ButtonLayout({
  size = 'md',
  display = 'all',
  styling = 'default',
  ...props
}) {
  const showIcons = display === 'all' || display === 'icons';
  const showText = display === 'all' || display === 'label';
  const labelClass = compStyles.label;
  const isOnlyIcon = props.startIcon && (display === 'icons' || !props.children) && !props.endIcon;
  const isEmbedded = styling === 'embedded';
  const textStartIcon = !isNullOrUndefined(props.startIcon) && display === 'all';
  const textEndIcon = !isNullOrUndefined(props.endIcon) && display === 'all';
  const isTwoIcons = !isNullOrUndefined(props.startIcon) && !isNullOrUndefined(props.endIcon);
  const isTwoIconsOnly = isTwoIcons && display === 'icons';
  const endIconClass = `${compStyles.icon} oj-c-button-layout-${size} ${compStyles.endIcon}`;
  const startIconStyles = classNames.classNames([compStyles.icon, isEmbedded ? `oj-c-button-layout-embedded-${size}` : `oj-c-button-layout-${size}`, !isOnlyIcon && compStyles.startIcon, isTwoIconsOnly && compStyles.startIconWhenBothIconsOnly]);
  const textStyles = classNames.classNames([compStyles.text, textStartIcon && !props.suffix && !isTwoIcons && compStyles.endText, textEndIcon && !isTwoIcons && compStyles.startText]);
  const suffixStyles = classNames.classNames([compStyles.suffix, textStartIcon && !isTwoIcons && compStyles.endText]);
  return jsxRuntime.jsxs("span", {
    class: labelClass,
    children: [showIcons && props.startIcon && jsxRuntime.jsx("span", {
      class: startIconStyles,
      children: props.startIcon
    }), showText && props.children && jsxRuntime.jsxs("span", {
      class: textStyles,
      children: [" ", props.children, " "]
    }), showText && props.suffix && jsxRuntime.jsx("span", {
      class: suffixStyles,
      children: jsxRuntime.jsx(Text.Text, {
        truncation: 'ellipsis',
        weight: 'inherit',
        children: props.suffix
      })
    }), showIcons && props.endIcon && jsxRuntime.jsx("span", {
      class: endIconClass,
      children: props.endIcon
    })]
  });
}

exports.ButtonLayout = ButtonLayout;
//# sourceMappingURL=ButtonLayout-ffa5087b.js.map
